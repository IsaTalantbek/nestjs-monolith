Если есть проблемы с производительностью, проблема скорее всего в:

LoggerInterceptor и FileLoggerService - возможно синхронны.

FileLoggerService - проблема с логикой создания и очищения логеров

Throuttler - Сохраняет в памяти блокировку, а не в redis
Mutex - Сохраняет в памяти блокировку, а не в redis
IpAdressBlock - Сохраняет в памяти блокировку, а не в redis

//

Появились непонятные ошибки? Скорее всего проблема в core.

LoggerService - есть вероятность неправильный настройки создание и удаление логера
LoggerInterceptor - используется перед и после каждого запроса
util/error - возможны проблемы, потому что экспортируется функция, а не класс
Mutex - Что-то мудрят в памяти
IpAdressBlock - Что-то мудрят в памяти

Возможно еще проблема в common

SessionGuard - это очень опасная зона, одна ошибка - и ты ошибся, потому что это глобальный гвард

Modules слишком изолированы, чтобы как-то влиять на проект. Если и 
случиться ошибка, то его будет легко найти

//

Не забыть:
В FileLoggerService убрать { deterministic: false }